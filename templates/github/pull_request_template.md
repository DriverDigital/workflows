## Summary
<!-- what changed and why -->

## Bonsai task
<!-- LINK the originating Bonsai GitHub issue so status-sync can resolve the task — add a line like
     "Closes #123" below (the issue body carries the Bonsai task URL the workflow reads). The status
     sync resolves the linked issue via the PR's closing reference, so a copied task URL here is not
     needed. AI-opened PRs are linked automatically via the issue's development branch. -->

## Testing
<!-- how this was verified -->

## Screenshots
<!-- if there are UI changes -->
